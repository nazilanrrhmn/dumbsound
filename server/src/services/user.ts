// import * as userRepositories from "../repositories/user";

// // Create a new user
// export const createUser = async (data: { email: string; password: string }) => {
//   return await userRepositories.createUser(data);
// };

// // Find a user by email
// export const findUserByEmail = async (email: string) => {
//   return await userRepositories.findUserByEmail(email);
// };

// // Find a user by id
// export const findUserById = async (id: number) => {
//   return await userRepositories.findUserById(id);
// };

// // Update user information
// export const updateUser = async (
//   id: number,
//   data: Partial<{ email: string; username: string }>
// ) => {
//   return await userRepositories.updateUser(id, data);
// };
